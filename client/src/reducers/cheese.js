import {FETCH_CHEESES_ERROR, FETCH_CHEESES_SUCCESS, FETCH_CHEESES_REQUEST} from '../actions';

const initialState = {
    cheeses: [],
    loading: false,
    error: null
}

export const CheesesReducer = (state = initialState, action) => {
  // if ()
}